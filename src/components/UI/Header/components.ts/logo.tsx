import styled from "styled-components";

export const Logo = 
styled.h1<{ width?: number, alignment?: string }>
`font-family: Righteous;
font-size: 50px;
color: #004F2D;
text-transform: uppercase;
font-weight: normal;
`;